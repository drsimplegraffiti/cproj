

 gcc processinghttp.c -o server #compile the server code
 gcc httpcrud.c -o client #compile the client code

Now you should have:
server
client


Run the Server (FIRST)
./server



Then in another terminal, run the client
./client


if you dont want to run the client, you can use curl to test the server
curl http://localhost:8080/
curl -X POST http://localhost:8080/api/items
curl -X PUT http://localhost:8080/api/items/1
curl -X DELETE http://localhost:8080/api/items/1
